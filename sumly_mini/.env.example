# Complete Go app API root. Domain clients append paths such as /auth/wechat/login.
VITE_API_BASE_URL=http://127.0.0.1:18090/api/v1/app

# Keep mock enabled for UI-only development; disable it to call the Go API.
VITE_USE_MOCK=true
