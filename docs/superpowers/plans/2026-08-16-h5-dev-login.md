# H5 开发测试登录 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为开发测试目的支持 H5 登录：后端新增 `DEV_LOGIN_ENABLED` 开关控制的 `POST /api/v1/app/auth/dev/login`（identifier 加 `dev-` 前缀当 openid，复用自动注册路径签发真实 JWT），前端 H5 首页提供"标识输入框 + 开发登录"入口。

**Architecture:** 沿用六边形架构：`auth` 模块新增 `DevLogin` application use case（依赖 `userports.Repository` + `ports.TokenService`），HTTP handler 增加可选 devLogin 依赖，仅开关启用时注册路由；前端在 `appSession` 增加 `loginWithDevIdentifier`，页面用 `#ifdef H5` 条件编译区分入口。

**Tech Stack:** Go 1.26.5 / Gin / JWT（后端）；uni-app + Vue 3 + TypeScript（前端）。

**Spec:** `docs/superpowers/specs/2026-08-16-h5-dev-login-design.md`

## Global Constraints

- 响应统一 `{ code, message, data }` envelope；`KindValidation` → HTTP 422（`internal/shared/adapters/httpapi/write.go`）。
- users 表 openid 为 `VARCHAR(128) UNIQUE`；dev 用户 openid = `"dev-" + identifier`，identifier 校验 ≤ 120 字符。
- Gin 只能出现在 `adapters/http` 和 `bootstrap`；`domain`/`application` 不依赖 Gin、pgx。
- 后端按 TDD 推进：先写失败测试，再写最小实现。
- Go 模块下载如需要：`GOPROXY=https://goproxy.cn,direct`，`GOSUMDB=sum.golang.google.cn`。
- 前端 mock 模式"全有或全无"：`isMockEnabled()` 为 true 时不渲染开发登录入口，不补 mock handler。
- 小程序端（`#ifndef H5`）行为完全不变。
- 不记录 JWT、openid、AppSecret、数据库连接串等敏感信息到日志。
- 每个 Task 结束按该 Task 的 commit 步骤提交；commit message 用中文 conventional 风格（参考 `docs: H5 开发测试登录设计文档`）。

---
---

### Task 1: 后端配置开关 `DEV_LOGIN_ENABLED`

**Files:**
- Modify: `sumly_go/internal/bootstrap/config.go`
- Test: `sumly_go/internal/bootstrap/config_test.go`（新文件）
- Modify: `sumly_go/.env.example`

**Interfaces:**
- Produces: `Config.DevLoginEnabled bool`（Task 4 消费）。

- [ ] **Step 1: 写失败测试**

创建 `sumly_go/internal/bootstrap/config_test.go`：

```go
package bootstrap

import "testing"

func TestLoadConfigParsesDevLoginEnabled(t *testing.T) {
	for _, test := range []struct {
		name     string
		value    string
		expected bool
	}{
		{name: "true 开启", value: "true", expected: true},
		{name: "false 关闭", value: "false", expected: false},
		{name: "缺省关闭", value: "", expected: false},
		{name: "其他值关闭", value: "1", expected: false},
	} {
		t.Run(test.name, func(t *testing.T) {
			t.Setenv("DATABASE_URL", "postgres://example")
			t.Setenv("JWT_SECRET", "secret")
			t.Setenv("WECHAT_APP_ID", "appid")
			t.Setenv("WECHAT_APP_SECRET", "appsecret")
			t.Setenv("DEV_LOGIN_ENABLED", test.value)

			config, err := LoadConfig()
			if err != nil {
				t.Fatalf("load config: %v", err)
			}
			if config.DevLoginEnabled != test.expected {
				t.Fatalf("expected DevLoginEnabled=%v, got %v", test.expected, config.DevLoginEnabled)
			}
		})
	}
}
```

说明：`configenv.Load()` 内部 `godotenv.Load()` 不覆盖已存在的环境变量，`t.Setenv` 设置的值优先，本地 `.env` 不干扰测试。

- [ ] **Step 2: 运行测试确认失败**

```bash
cd sumly_go && go test ./internal/bootstrap/ -run TestLoadConfigParsesDevLoginEnabled -v
```

预期：FAIL，编译错误 `unknown field 'DevLoginEnabled' in struct literal of type Config` 或断言失败。

- [ ] **Step 3: 最小实现**

`sumly_go/internal/bootstrap/config.go`：`Config` 结构体增加字段（放在 `AppEnvironment` 之后）：

```go
type Config struct {
	HTTPAddr        string
	DatabaseURL     string
	JWTSecret       string
	WechatAppID     string
	WechatAppSecret string
	AppEnvironment  AppEnvironment
	DevLoginEnabled bool
}
```

`LoadConfig` 的 config 字面量增加一行：

```go
		DevLoginEnabled: os.Getenv("DEV_LOGIN_ENABLED") == "true",
```

`DEV_LOGIN_ENABLED` 不加入必填校验 map。

- [ ] **Step 4: 运行测试确认通过**

```bash
cd sumly_go && go test ./internal/bootstrap/ -run TestLoadConfigParsesDevLoginEnabled -v
```

预期：PASS。

- [ ] **Step 5: 更新 .env.example**

`sumly_go/.env.example` 在 `APP_ENV=development` 行后追加：

```
# 开发测试专用：开启后注册 POST /api/v1/app/auth/dev/login（任意 identifier 直接换取 JWT），
# 仅用于本地 H5 联调，生产环境必须保持关闭（缺省/非 true 即为关闭）。
DEV_LOGIN_ENABLED=true
```

- [ ] **Step 6: Commit**

```bash
cd sumly_go && gofmt -w . && go vet ./internal/bootstrap/
git add sumly_go/internal/bootstrap/config.go sumly_go/internal/bootstrap/config_test.go sumly_go/.env.example
git commit -m "feat(go): 增加 DEV_LOGIN_ENABLED 开发登录开关配置"
```

---

### Task 2: `DevLogin` application use case

**Files:**
- Create: `sumly_go/internal/auth/application/dev_login.go`
- Test: `sumly_go/internal/auth/application/dev_login_test.go`

**Interfaces:**
- Consumes: `userports.Repository`（`FindByOpenID(context.Context, string) (userdomain.User, bool, error)`、`Create(context.Context, userdomain.User) (userdomain.User, error)`）；`ports.TokenService`（`IssueUser(context.Context, int64) (string, error)`）；`userdomain.NewUser(openID string) (User, error)`、`User.IsActive() bool`。
- Produces: `NewDevLogin(users userports.Repository, tokens ports.TokenService) DevLogin`；`DevLogin.Execute(ctx context.Context, identifier string) (DevLoginResult, error)`；`DevLoginResult{ Token string; User userdomain.User }`（Task 3、4 消费）。
- 测试复用 `wechat_login_test.go` 中已有的 `newFakeUsers` / `fakeTokenService`（同 package `application`，不要重复定义）。

- [ ] **Step 1: 写失败测试**

创建 `sumly_go/internal/auth/application/dev_login_test.go`：

```go
package application

import (
	"context"
	"strings"
	"testing"

	userdomain "gitee.com/oryjk/sumly/sumly_go/internal/user/domain"
)

func TestDevLoginCreatesMissingUserWithPrefixedOpenID(t *testing.T) {
	users := newFakeUsers()
	tokens := &fakeTokenService{token: "jwt-1"}
	useCase := NewDevLogin(users, tokens)

	result, err := useCase.Execute(context.Background(), "test-user-01")
	if err != nil {
		t.Fatalf("execute dev login: %v", err)
	}
	if result.Token != "jwt-1" {
		t.Fatalf("expected jwt-1, got %s", result.Token)
	}
	if users.created.OpenID != "dev-test-user-01" {
		t.Fatalf("expected created openid dev-test-user-01, got %s", users.created.OpenID)
	}
}

func TestDevLoginReturnsExistingUserWithoutRecreating(t *testing.T) {
	users := newFakeUsers()
	users.byOpenID["dev-test-user-01"] = userdomain.User{ID: 9, OpenID: "dev-test-user-01", Status: userdomain.StatusActive}
	useCase := NewDevLogin(users, &fakeTokenService{token: "jwt-1"})

	result, err := useCase.Execute(context.Background(), "test-user-01")
	if err != nil {
		t.Fatalf("execute dev login: %v", err)
	}
	if result.User.ID != 9 {
		t.Fatalf("expected existing user 9, got %d", result.User.ID)
	}
	if users.created.OpenID != "" {
		t.Fatalf("expected no user creation, got %+v", users.created)
	}
}

func TestDevLoginRejectsEmptyIdentifier(t *testing.T) {
	useCase := NewDevLogin(newFakeUsers(), &fakeTokenService{token: "jwt-1"})

	if _, err := useCase.Execute(context.Background(), "  "); err == nil {
		t.Fatal("expected empty identifier to fail")
	}
}

func TestDevLoginRejectsTooLongIdentifier(t *testing.T) {
	useCase := NewDevLogin(newFakeUsers(), &fakeTokenService{token: "jwt-1"})

	if _, err := useCase.Execute(context.Background(), strings.Repeat("a", 121)); err == nil {
		t.Fatal("expected identifier longer than 120 characters to fail")
	}
}

func TestDevLoginRejectsFrozenUser(t *testing.T) {
	users := newFakeUsers()
	users.byOpenID["dev-test-user-01"] = userdomain.User{ID: 9, OpenID: "dev-test-user-01", Status: userdomain.StatusFrozen}
	useCase := NewDevLogin(users, &fakeTokenService{token: "jwt-1"})

	if _, err := useCase.Execute(context.Background(), "test-user-01"); err == nil {
		t.Fatal("expected frozen user login to fail")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd sumly_go && go test ./internal/auth/application/ -run TestDevLogin -v
```

预期：FAIL，编译错误 `undefined: NewDevLogin`。

- [ ] **Step 3: 最小实现**

创建 `sumly_go/internal/auth/application/dev_login.go`：

```go
package application

import (
	"context"
	"strings"
	"unicode/utf8"

	"gitee.com/oryjk/sumly/sumly_go/internal/auth/ports"
	sharederror "gitee.com/oryjk/sumly/sumly_go/internal/shared/domain"
	userdomain "gitee.com/oryjk/sumly/sumly_go/internal/user/domain"
	userports "gitee.com/oryjk/sumly/sumly_go/internal/user/ports"
)

const (
	devOpenIDPrefix         = "dev-"
	maxDevIdentifierLength  = 120
)

// DevLogin 是仅开发测试使用的登录：identifier 加 dev- 前缀作为 openid，
// 复用与微信登录一致的"查不到自动注册"路径。仅 DEV_LOGIN_ENABLED=true 时挂载路由。
type DevLogin struct {
	users  userports.Repository
	tokens ports.TokenService
}

type DevLoginResult struct {
	Token string
	User  userdomain.User
}

func NewDevLogin(users userports.Repository, tokens ports.TokenService) DevLogin {
	return DevLogin{users: users, tokens: tokens}
}

func (u DevLogin) Execute(ctx context.Context, identifier string) (DevLoginResult, error) {
	identifier = strings.TrimSpace(identifier)
	if identifier == "" {
		return DevLoginResult{}, sharederror.New(sharederror.KindValidation, "开发登录标识不能为空")
	}
	if utf8.RuneCountInString(identifier) > maxDevIdentifierLength {
		return DevLoginResult{}, sharederror.New(sharederror.KindValidation, "开发登录标识不能超过 120 个字符")
	}
	openID := devOpenIDPrefix + identifier
	user, found, err := u.users.FindByOpenID(ctx, openID)
	if err != nil {
		return DevLoginResult{}, sharederror.Wrap(sharederror.KindInternal, "查询用户失败", err)
	}
	if !found {
		user, err = userdomain.NewUser(openID)
		if err != nil {
			return DevLoginResult{}, err
		}
		user, err = u.users.Create(ctx, user)
		if err != nil {
			return DevLoginResult{}, sharederror.Wrap(sharederror.KindInternal, "创建用户失败", err)
		}
	}
	if !user.IsActive() {
		return DevLoginResult{}, sharederror.New(sharederror.KindForbidden, "用户已冻结")
	}
	token, err := u.tokens.IssueUser(ctx, user.ID)
	if err != nil {
		return DevLoginResult{}, sharederror.Wrap(sharederror.KindInternal, "签发登录凭证失败", err)
	}
	return DevLoginResult{Token: token, User: user}, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd sumly_go && go test ./internal/auth/application/ -v
```

预期：全部 PASS（含原有 `TestWechatLogin*`）。

- [ ] **Step 5: Commit**

```bash
cd sumly_go && gofmt -w . && go vet ./internal/auth/...
git add sumly_go/internal/auth/application/dev_login.go sumly_go/internal/auth/application/dev_login_test.go
git commit -m "feat(go): 新增开发测试登录 use case"
```

---

### Task 3: HTTP handler 与路由注册

**Files:**
- Modify: `sumly_go/internal/auth/adapters/http/handler.go`
- Test: `sumly_go/internal/auth/adapters/http/handler_test.go`
- Test: `sumly_go/internal/bootstrap/router_test.go`

**Interfaces:**
- Consumes: Task 2 的 `application.DevLoginResult`、`DevLogin.Execute(ctx, identifier)`。
- Produces: `authhttp.NewHandler(wechatLogin WechatLoginUseCase, devLogin DevLoginUseCase) *Handler`（**签名变更**，Task 4 与所有调用点同步更新）；`DevLoginUseCase` 接口；路由 `POST /auth/dev/login`（仅 `devLogin != nil` 时注册）。

- [ ] **Step 1: 写失败测试（handler）**

`sumly_go/internal/auth/adapters/http/handler_test.go` 追加：

```go
func TestDevLoginHandlerReturnsTokenAndUser(t *testing.T) {
	gin.SetMode(gin.TestMode)
	handler := NewHandler(&fakeWechatLogin{}, &fakeDevLogin{result: application.DevLoginResult{
		Token: "jwt-dev",
		User:  userdomain.User{ID: 7, OpenID: "dev-test-user-01", Status: userdomain.StatusActive},
	}})
	router := gin.New()
	router.POST("/dev-login", handler.DevLogin)
	request := httptest.NewRequest(http.MethodPost, "/dev-login", bytes.NewBufferString(`{"identifier":"test-user-01"}`))
	request.Header.Set("Content-Type", "application/json")
	response := httptest.NewRecorder()

	router.ServeHTTP(response, request)

	if response.Code != http.StatusOK {
		t.Fatalf("expected status %d, got %d: %s", http.StatusOK, response.Code, response.Body.String())
	}
	if !bytes.Contains(response.Body.Bytes(), []byte(`"token":"jwt-dev"`)) {
		t.Fatalf("expected token in response: %s", response.Body.String())
	}
}

func TestDevLoginHandlerRejectsEmptyBody(t *testing.T) {
	gin.SetMode(gin.TestMode)
	handler := NewHandler(&fakeWechatLogin{}, &fakeDevLogin{})
	router := gin.New()
	router.POST("/dev-login", handler.DevLogin)
	response := httptest.NewRecorder()
	router.ServeHTTP(response, httptest.NewRequest(http.MethodPost, "/dev-login", nil))

	if response.Code != http.StatusUnprocessableEntity {
		t.Fatalf("expected status %d, got %d: %s", http.StatusUnprocessableEntity, response.Code, response.Body.String())
	}
}

type fakeDevLogin struct {
	result application.DevLoginResult
	err    error
}

func (f *fakeDevLogin) Execute(context.Context, string) (application.DevLoginResult, error) {
	return f.result, f.err
}
```

同时把文件里两处既有 `NewHandler(...)` 调用改为两参数形式：

- `NewHandler(&fakeWechatLogin{result: ...})` → `NewHandler(&fakeWechatLogin{result: ...}, nil)`
- `NewHandler(&fakeWechatLogin{})` → `NewHandler(&fakeWechatLogin{}, nil)`

- [ ] **Step 2: 写失败测试（router）**

`sumly_go/internal/bootstrap/router_test.go` 追加：

```go
func TestDevLoginRouteRegisteredOnlyWhenEnabled(t *testing.T) {
	gin.SetMode(gin.TestMode)
	enabled := NewRouter(Dependencies{UserAuth: authhttp.NewHandler(routerWechatLogin{}, routerDevLogin{})})
	enabledResp := httptest.NewRecorder()
	enabled.ServeHTTP(enabledResp, httptest.NewRequest(http.MethodPost, "/api/v1/app/auth/dev/login", nil))
	if enabledResp.Code != http.StatusUnprocessableEntity {
		t.Fatalf("expected dev login route to exist with validation status %d, got %d", http.StatusUnprocessableEntity, enabledResp.Code)
	}

	disabled := NewRouter(Dependencies{UserAuth: authhttp.NewHandler(routerWechatLogin{}, nil)})
	disabledResp := httptest.NewRecorder()
	disabled.ServeHTTP(disabledResp, httptest.NewRequest(http.MethodPost, "/api/v1/app/auth/dev/login", nil))
	if disabledResp.Code != http.StatusNotFound {
		t.Fatalf("expected dev login route to be absent (404), got %d", disabledResp.Code)
	}
}

type routerDevLogin struct{}

func (routerDevLogin) Execute(context.Context, string) (authapplication.DevLoginResult, error) {
	return authapplication.DevLoginResult{}, nil
}
```

同时把既有 `TestWechatLoginRouteIsPublicAndVersioned` 中的
`authhttp.NewHandler(routerWechatLogin{})` 改为 `authhttp.NewHandler(routerWechatLogin{}, nil)`。

- [ ] **Step 3: 运行测试确认失败**

```bash
cd sumly_go && go test ./internal/auth/adapters/http/ ./internal/bootstrap/ -v
```

预期：FAIL，编译错误（`NewHandler` 参数数量不匹配、`handler.DevLogin` 未定义、`application.DevLoginResult` 相关未定义已实现所以主要是 handler 侧）。

- [ ] **Step 4: 最小实现**

`sumly_go/internal/auth/adapters/http/handler.go` 修改：

```go
type DevLoginUseCase interface {
	Execute(context.Context, string) (application.DevLoginResult, error)
}

type Handler struct {
	wechatLogin WechatLoginUseCase
	devLogin    DevLoginUseCase
}

type DevLoginRequest struct {
	Identifier string `json:"identifier" binding:"required"`
}

func NewHandler(wechatLogin WechatLoginUseCase, devLogin DevLoginUseCase) *Handler {
	return &Handler{wechatLogin: wechatLogin, devLogin: devLogin}
}

func (h *Handler) DevLogin(c *gin.Context) {
	var request DevLoginRequest
	if err := c.ShouldBindJSON(&request); err != nil {
		sharedhttpapi.WriteError(c, sharederror.New(sharederror.KindValidation, "开发登录标识不能为空"))
		return
	}
	result, err := h.devLogin.Execute(c.Request.Context(), request.Identifier)
	if err != nil {
		sharedhttpapi.WriteError(c, err)
		return
	}
	sharedhttpapi.WriteSuccess(c, WechatLoginResponse{
		Token: result.Token,
		User:  mapUserResponse(result.User),
	})
}

func (h *Handler) RegisterPublicRoutes(group *gin.RouterGroup) {
	group.POST("/auth/wechat/login", h.WechatLogin)
	if h.devLogin != nil {
		group.POST("/auth/dev/login", h.DevLogin)
	}
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd sumly_go && go test ./internal/auth/... ./internal/bootstrap/ -v
```

预期：全部 PASS。

- [ ] **Step 6: Commit**

```bash
cd sumly_go && gofmt -w . && go vet ./internal/...
git add sumly_go/internal/auth/adapters/http/handler.go sumly_go/internal/auth/adapters/http/handler_test.go sumly_go/internal/bootstrap/router_test.go
git commit -m "feat(go): 挂载 dev 登录 HTTP 接口（开关关闭时不注册路由）"
```

---

### Task 4: bootstrap 接线与启动 warn 日志

**Files:**
- Modify: `sumly_go/internal/bootstrap/dependencies.go`

**Interfaces:**
- Consumes: Task 1 的 `Config.DevLoginEnabled`；Task 2 的 `authapplication.NewDevLogin`；Task 3 的 `authhttp.NewHandler(wechatLogin, devLogin)`。

- [ ] **Step 1: 修改接线**

`sumly_go/internal/bootstrap/dependencies.go`：

import 增加 `"log/slog"`。

把

```go
	wechatLogin := authapplication.NewWechatLogin(wechatClient, userRepository, tokens)
	userAuthHandler := authhttp.NewHandler(wechatLogin)
```

改为

```go
	wechatLogin := authapplication.NewWechatLogin(wechatClient, userRepository, tokens)
	var devLogin authhttp.DevLoginUseCase
	if config.DevLoginEnabled {
		// 纵深防御：dev 登录允许任意 identifier 直接换取 JWT，启用必须在日志中可见。
		slog.Warn("dev login endpoint is enabled (DEV_LOGIN_ENABLED=true); never enable in production")
		login := authapplication.NewDevLogin(userRepository, tokens)
		devLogin = login
	}
	userAuthHandler := authhttp.NewHandler(wechatLogin, devLogin)
```

注意：`devLogin` 必须声明为接口类型 `authhttp.DevLoginUseCase`，不要把未启用的场景写成类型化 nil 指针（会导致接口非 nil、路由被误注册）。

- [ ] **Step 2: 全量验证**

```bash
cd sumly_go && gofmt -w . && go test ./... && go vet ./... && go build -o /tmp/sumly-go-api ./cmd/api
```

预期：全部通过。

- [ ] **Step 3: Commit**

```bash
git add sumly_go/internal/bootstrap/dependencies.go
git commit -m "feat(go): 按开关接线 dev 登录并打启动 warn 日志"
```

---

### Task 5: OpenAPI 文档

**Files:**
- Modify: `sumly_go/docs/openapi.yaml`

- [ ] **Step 1: 补充端点定义**

`paths:` 中在 `/api/v1/app/auth/wechat/login` 块之后插入：

```yaml
  /api/v1/app/auth/dev/login:
    post:
      operationId: loginAppWithDevIdentifier
      tags: [App Auth]
      summary: 开发测试登录（仅开发环境）
      description: 仅开发测试使用，需后端 DEV_LOGIN_ENABLED=true，未启用时该路由不注册（404）。identifier 加 dev- 前缀作为 openid，用户不存在时自动注册并签发 JWT。生产环境禁止开启。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DevLoginRequest"
      responses:
        "200":
          description: 登录成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WechatLoginResponse"
        "403":
          description: 用户已冻结
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: 请求参数无效
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
```

`components.schemas` 中在 `WechatLoginRequest` 块之后插入：

```yaml
    DevLoginRequest:
      type: object
      required: [identifier]
      properties:
        identifier:
          type: string
          maxLength: 120
          description: 开发测试标识（如 test-user-01），后端加 dev- 前缀作为 openid
```

- [ ] **Step 2: 验证**

```bash
cd sumly_go && go test ./docs/ -v
```

预期：PASS（`openapi_test.go` 校验 yaml 可解析）。

- [ ] **Step 3: Commit**

```bash
git add sumly_go/docs/openapi.yaml
git commit -m "docs(go): openapi 补充 dev 登录接口定义"
```

---

### Task 6: 前端 API 封装与会话逻辑

**Files:**
- Modify: `sumly_mini/src/api/auth.ts`
- Modify: `sumly_mini/src/stores/appSession.ts`

**Interfaces:**
- Consumes: `requestApi<T>({ url, method, data })`（`src/utils/request.ts`）；`LoginResponse`（`src/types/app.ts`）。
- Produces: `devLogin(identifier: string): Promise<LoginResponse>`；`loginWithDevIdentifier(identifier: string): Promise<void>`（Task 7 消费）。

- [ ] **Step 1: 新增 api 封装**

`sumly_mini/src/api/auth.ts` 追加：

```ts
export function devLogin(identifier: string) {
  return requestApi<LoginResponse>({
    url: "/auth/dev/login",
    method: "POST",
    data: { identifier },
  });
}
```

- [ ] **Step 2: 新增会话动作**

`sumly_mini/src/stores/appSession.ts`：

import 区把 `import { wechatLogin } from "@/api/auth";` 改为：

```ts
import { devLogin, wechatLogin } from "@/api/auth";
```

在 `ensureSessionReady` 之后追加（放在 `applyCurrentUser` 前）：

```ts
/**
 * H5 开发测试登录：identifier 换取真实 JWT（后端需 DEV_LOGIN_ENABLED=true）。
 * 仅供开发调试；mock 模式与小程序端不使用。
 */
export async function loginWithDevIdentifier(identifier: string) {
  // 使任何进行中的 bootstrap 失效，避免旧会话覆盖新登录结果
  sessionVersion += 1;
  clearManualLogout();
  const loginVersion = sessionVersion;

  const result = await devLogin(identifier);
  assertSessionVersion(loginVersion);
  setAccessToken(result.token);
  assertSessionVersion(loginVersion);
  currentUser.value = result.user;
}
```

- [ ] **Step 3: 类型检查**

```bash
cd sumly_mini && bun run type-check
```

预期：通过。

- [ ] **Step 4: Commit**

```bash
git add sumly_mini/src/api/auth.ts sumly_mini/src/stores/appSession.ts
git commit -m "feat(mini): 新增 H5 开发登录 api 封装与会话动作"
```

---

### Task 7: 前端页面入口

**Files:**
- Modify: `sumly_mini/src/pages/home/index.vue`
- Modify: `sumly_mini/src/pages/user/index.vue`

**Interfaces:**
- Consumes: Task 6 的 `loginWithDevIdentifier`；既有 `isMockEnabled()`（`@/mock`）。

- [ ] **Step 1: 首页 script 修改**

`sumly_mini/src/pages/home/index.vue` script 区：

import 改为：

```ts
import { computed, ref } from "vue";
import { ensureSessionReady, clearSession, loginWithDevIdentifier, useAppSession } from "@/stores/appSession";
import { isMockEnabled } from "@/mock";
```

（`ref` 为新增引入。）

`handleLogin` 之后追加：

```ts
const devIdentifier = ref("");
const isDevLoggingIn = ref(false);

async function handleDevLogin() {
  const identifier = devIdentifier.value.trim();
  if (!identifier) {
    uni.showToast({ title: "请输入测试标识", icon: "none" });
    return;
  }
  if (isDevLoggingIn.value) {
    return;
  }
  isDevLoggingIn.value = true;
  try {
    await loginWithDevIdentifier(identifier);
    uni.$emit("session:login-completed");
  } catch (error) {
    uni.showToast({ title: error instanceof Error ? error.message : "登录失败", icon: "none" });
  } finally {
    isDevLoggingIn.value = false;
  }
}
```

- [ ] **Step 2: 首页 template 修改**

把现有的

```vue
        <button v-if="!isLoggedIn" class="app-primary-button home-action" @click="handleLogin">
          微信登录
        </button>
```

替换为：

```vue
        <!-- #ifndef H5 -->
        <button v-if="!isLoggedIn" class="app-primary-button home-action" @click="handleLogin">
          微信登录
        </button>
        <!-- #endif -->
        <!-- #ifdef H5 -->
        <template v-if="!isLoggedIn">
          <button v-if="isMockEnabled()" class="app-primary-button home-action" @click="handleLogin">
            登录（mock）
          </button>
          <view v-else class="home-dev-login">
            <input
              v-model="devIdentifier"
              class="home-dev-input"
              placeholder="测试标识，如 test-user-01"
              maxlength="120"
            />
            <button class="app-primary-button home-action" :disabled="isDevLoggingIn" @click="handleDevLogin">
              {{ isDevLoggingIn ? "登录中..." : "开发登录" }}
            </button>
          </view>
        </template>
        <!-- #endif -->
```

把底部的 hint：

```vue
      <view class="app-hint">
        {{ isMockEnabled() ? "当前为 mock 模式（VITE_USE_MOCK=true），登录不请求真实后端。" : "登录走 sumly_go 后端 POST /api/v1/app/auth/wechat/login。" }}
      </view>
```

替换为：

```vue
      <!-- #ifndef H5 -->
      <view class="app-hint">
        {{ isMockEnabled() ? "当前为 mock 模式（VITE_USE_MOCK=true），登录不请求真实后端。" : "登录走 sumly_go 后端 POST /api/v1/app/auth/wechat/login。" }}
      </view>
      <!-- #endif -->
      <!-- #ifdef H5 -->
      <view class="app-hint">
        {{ isMockEnabled() ? "当前为 mock 模式（VITE_USE_MOCK=true），登录不请求真实后端。" : "H5 开发登录走 POST /api/v1/app/auth/dev/login（后端需 DEV_LOGIN_ENABLED=true）。" }}
      </view>
      <!-- #endif -->
```

- [ ] **Step 3: 首页样式追加**

`sumly_mini/src/pages/home/index.vue` style 区追加：

```css
.home-dev-login {
  margin-top: 24rpx;
}

.home-dev-input {
  background: #f3f4f6;
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  font-size: 28rpx;
}
```

- [ ] **Step 4: user 页提示文案按平台区分**

`sumly_mini/src/pages/user/index.vue` 把：

```vue
      <view class="app-hint">请返回首页点击「微信登录」后再查看资料。</view>
```

替换为：

```vue
      <!-- #ifndef H5 -->
      <view class="app-hint">请返回首页点击「微信登录」后再查看资料。</view>
      <!-- #endif -->
      <!-- #ifdef H5 -->
      <view class="app-hint">请返回首页使用开发登录后再查看资料。</view>
      <!-- #endif -->
```

- [ ] **Step 5: 验证**

```bash
cd sumly_mini && bun run type-check && bun run build:h5
```

预期：type-check 通过；`build:h5` 构建成功（验证 `#ifdef` 条件编译与模板语法正确）。

- [ ] **Step 6: Commit**

```bash
git add sumly_mini/src/pages/home/index.vue sumly_mini/src/pages/user/index.vue
git commit -m "feat(mini): H5 首页增加开发登录入口"
```

---

### Task 8: 手动联调验证（无代码变更，不提交）

- [ ] **Step 1: 本地 .env 开启开关**

在 `sumly_go/.env` 追加一行（不读取该文件其他内容）：

```bash
echo 'DEV_LOGIN_ENABLED=true' >> sumly_go/.env
```

- [ ] **Step 2: 启动后端并确认 warn 日志**

```bash
cd sumly_go && make run
```

预期：启动日志出现 `dev login endpoint is enabled` warn。

- [ ] **Step 3: 启动 H5 并走查**

```bash
cd sumly_mini && bun run dev:h5
```

浏览器打开后逐项确认：

1. 游客态显示标识输入框 + 开发登录按钮（不显示"微信登录"按钮）；
2. 空标识点击 → toast「请输入测试标识」，不发请求；
3. 输入 `test-user-01` 登录 → 显示用户 ID 与状态；
4. 「查看我的资料」→ 修改昵称保存成功；
5. 刷新页面 → 会话保持（token 自动恢复）；
6. 退出登录 → 换 `test-user-02` 再登 → 是另一个用户 ID（多用户场景）；
7. 关掉后端 `.env` 的开关重启（或注释掉）→ 开发登录请求返回 404。

- [ ] **Step 4: 全量回归**

```bash
cd sumly_go && gofmt -w . && go test ./... && go vet ./... && go build -o /tmp/sumly-go-api ./cmd/api
cd sumly_mini && bun run type-check && bun run build:mp-weixin
```

预期：全部通过（`build:mp-weixin` 确认小程序端条件编译不受影响）。

---

## 收尾

全部 Task 完成后，如 `AGENTS.md` 涉及内容有变化（本次新增了 dev 登录接口与开关，建议在 `sumly_go/AGENTS.md` 架构约束或 `sumly_mini/AGENTS.md` 协作约定中补一句 dev 登录说明），单独一个 commit 更新：

```bash
git commit -m "docs: AGENTS 补充 dev 登录说明"
```
